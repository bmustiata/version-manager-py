# self
version:
  version: 2.0.15
  files:
    setup.py: version='**VERSION**',
    README.md: '# vm **VERSION$$'
usage:
  version: $(version-manager --help)
  files:
    README.md: (Help:\n\n```text\n)(.*?)(\n```)
